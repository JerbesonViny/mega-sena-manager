#include "integers.h"

int left_values_is_greater_than_right_value(int left_value, int right_value)
{
    return left_value > right_value;
}

int left_values_is_less_than_right_value(int left_value, int right_value)
{
    return left_value < right_value;
}